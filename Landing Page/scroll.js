document.lastScrollPosition = 0;
document.addEventListener("scroll", () => {
  const direction =
    window.apgeYOffset - document.lastScrollPosition > 0 ? "down" : "up";

  const sections = (document.document.lastScrollPosition = window.pageYOffset);
});
